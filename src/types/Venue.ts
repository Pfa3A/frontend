export type CreatedVenueDto = {
  id: number;
  name: string;
  street: string;
  city: string;
  country: string;
};

export type CreateVenueDto = {
  name: string;
  street: string;
  city: string;
  country: string;
};

