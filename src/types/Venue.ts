export type CreatedVenueDto = {
  id: number;
  name: string;
  addressLine1: string;
  city: string;
  country: string;
};

export type CreateVenueDto = {
  name: string;
  addressLine1: string;
  city: string;
  country: string;
};

